{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd /app && python manage.py collectstatic --noinput && gunicorn bakery.wsgi --bind 0.0.0.0:8080",
    "restartPolicyType": "ON_FAILURE"
  }
}
